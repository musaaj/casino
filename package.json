{
  "devDependencies": {
    "http-server": "^14.1.1",
    "tailwindcss": "^3.3.3"
  },
  "scripts": {
    "esbuild": "esbuild src/main.ts --bundle --outfile=script.js --target=es2017 --minify --watch=forever --serve --servedir=.",
    "serve": "http-server -a localhost -c 1",
    "tailwind": "npx tailwind -i input.css -o style.css -m -w",
    "build": "./start.sh"
  },
  "dependencies": {
    "animejs": "^3.2.1",
    "fb-watchman": "^2.0.2",
    "html-minifier": "^4.0.0",
    "prettier": "^3.0.3",
    "watchman": "^1.0.0"
  }
}
